# HELIX Waitlist Landing Page — Environment Variables
# Copy this file to .env.local and fill in your values

# ── SENTRY ──────────────────────────────────────────────────────────
# Get DSN from: sentry.io → Project → Settings → Client Keys
NEXT_PUBLIC_SENTRY_DSN=https://YOUR_KEY@oXXXXXX.ingest.sentry.io/XXXXXXX

# For source map uploads during build (shows readable stack traces in Sentry)
# Get auth token from: sentry.io → Settings → Auth Tokens → Create new token
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=helix-landing
SENTRY_AUTH_TOKEN=your_auth_token_here

# ── RESEND — Email Notifications (optional) ─────────────────────────
# Get a free API key at: https://resend.com/api-keys (3,000 emails/mo free)
# If not set, signups are still captured in Vercel logs
RESEND_API_KEY=re_your_api_key_here

# Email address to receive new waitlist signup notifications
RESEND_TO_EMAIL=your@email.com

# ── HELIX — Waitlist Counter (social proof) ─────────────────────────
# Manually update this in Vercel env vars to show social proof on the hero section
# Only shown when > 0. Format: integer (e.g. 42)
WAITLIST_COUNT=0
