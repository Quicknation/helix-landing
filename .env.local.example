# HELIX Waitlist Landing Page — Environment Variables
# Copy this file to .env.local and fill in your values

# ── Resend Email Notifications (optional) ──────────────────────────
# Get a free API key at: https://resend.com/api-keys (3,000 emails/mo free)
# If not set, signups are still captured in Vercel logs
RESEND_API_KEY=re_your_api_key_here

# Email address to receive new waitlist signup notifications
RESEND_TO_EMAIL=your@email.com

# ── Waitlist Counter (social proof) ────────────────────────────────
# Manually update this number to show social proof on the hero section
# Only shown when > 0. Format: integer (e.g. 42)
WAITLIST_COUNT=0
