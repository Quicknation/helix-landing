import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "HELIX — The AI Platform That Closes Every Gap",
  description:
    "Two worlds. One intelligence. HELIX is the AI platform built to keep every promise that every other AI tool has broken. Join the waitlist.",
  openGraph: {
    title: "HELIX — The AI Platform That Closes Every Gap",
    description: "Two worlds. One intelligence. Join the waitlist.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
